git config --global user.name "Tatara"
git config --global user.email rabumel.dpl@gmail.com

default path setting

ワークツリー(登録)インデックス(コミット)ローカルリポジトリ(プッシュ)リモートリポジトリ

git init ディレクトリにリポジトリを作成

git add ... インデックスに登録
git commit -m "comment"　コミット
git remote add origin git@github.com:R-Tatara/test.git
git push -u origin master

git clone https://
git pull https://


git status 変更内容の表示
git log コミットログの表示
git config 使用方法の表示


echo "# test1" >> readme.md
git init
git add read.md
git commit -m "first commit"
git remote add origin https://github.com/R-Tatara/test1.git
git push -u origin master

echo "" >> .txt 文末に挿入



